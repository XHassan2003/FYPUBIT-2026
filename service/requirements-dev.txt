# Test-only dependencies. The service itself needs nothing here — install these
# on top of requirements.txt when you want to run the suite:
#
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest

pytest>=8.0,<9.0
# FastAPI's TestClient drives the app in-process. Starlette 1.x wants httpx2;
# the older httpx still works but warns on every run.
httpx2>=2.0,<3.0
